import React from 'react'
import { useTranslations } from 'next-intl'
import Hero from '@/components/shared/hero'

export default function ManagementPage() {
  return (
    <main>
      <Hero title={} subtitle={} buttonText={} buttonLink={} imageSrc={} />
    </main>
  )
}
